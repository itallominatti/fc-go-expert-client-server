version: "3.8"

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - cotacoes-data:/app/data
    environment:
      - DB_PATH=/app/data/cotacoes.db
    restart: unless-stopped

volumes:
  cotacoes-data:
    driver: local
